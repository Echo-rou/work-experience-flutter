# 工作经验库：项目完整总结

## 项目定位

工作经验库是一个本地优先的个人经验记录工具。Windows 端为 Flutter 桌面应用，同时在电脑上提供局域网 HTTPS 服务；iPhone 使用可添加到主屏幕的 PWA。两台设备在同一局域网时自动同步，离线时各自继续使用本地副本。

## 已完成功能

- Windows 独立桌面应用，采用横向宽屏英文界面。
- 经验记录的新建、编辑、搜索、标签、分类、Markdown 阅读和回收站。
- 显示记录天数，不显示连续记录天数、无价值的收藏统计和冗余数据统计。
- Windows 数据采用本地 JSON，并保留写入前备份。
- iPhone PWA 使用 IndexedDB，本地离线可查看和编辑。
- `.dwr` 数据导入和导出。
- 同一局域网双向同步，以记录更新时间解决版本冲突，并传播删除墓碑。
- Windows 自动创建应用专用根证书，通过 HTTPS 提供 PWA 和同步接口。

## 安全设计

- 无秘密的 HTTP 页面只负责提供公共根证书和安全配对入口。
- 首次连接必须在 HTTPS 页面输入桌面端显示的 8 位配对码。
- 同步访问密钥为 128 位随机值，不放在安装链接中。
- 配对和认证失败均有频率限制，密钥使用恒定时间比较。
- 响应包含 CSP、防嵌入、权限限制、来源策略和 MIME 嗅探保护头。
- PFX 密码每次随机生成，PFX 加载后立即删除；可导出的临时服务器证书也会从证书库清理。
- 导入的数据标识符经过限制，外部链接仅允许 HTTP/HTTPS，避免脚本链接和 HTML 属性注入。
- 高级同步只接受 HTTPS；HTTP 仅允许 localhost 调试。

## 已完成验证

- Flutter 单元测试全部通过。
- Windows Release 构建成功。
- OSV 查询 70 个锁定依赖，没有发现已知漏洞（查询日期：2026-08-04）。
- 未授权访问返回 401。
- 连续错误配对的第 6 次请求返回 429。
- HTTP 安装页不包含同步密钥或旧式 `?k=` 参数。
- TLS 启动后没有临时 PFX或可导出叶证书残留。

## 数据与隐私边界

PWA 的 Screen Lock 只是界面锁，不是加密。Windows JSON、iPhone IndexedDB 和 `.dwr` 备份都没有应用级加密。使用者应开启 Windows 登录密码、BitLocker/设备加密和 iPhone 强密码，并避免把未加密备份放进公共网盘或 Git 仓库。

## 发布状态

当前版本适合个人使用和交付测试。正式商业分发前建议补充 Windows 代码签名、独立隐私政策、真实设备验收和应用级敏感数据加密。

## 仓库内容

- `lib/`：Flutter 应用、数据模型、本地存储、局域网同步和 UI。
- `assets/pwa/`：iPhone PWA 页面、清单、图标和 Service Worker。
- `windows/`、`ios/`、`macos/`：平台工程。
- `test/`：数据模型和 HTTPS 传输策略测试。
- `tool/security_runtime_check.py`：本机运行时安全检查。
- `README.md`：安装、运行、安全和隐私说明。

构建产物、缓存、证书、密钥和个人数据不应提交到 Git。
